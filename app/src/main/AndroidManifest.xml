<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:largeHeap="true"
        android:multiprocess="true"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Choirfinder"
        tools:targetApi="31" >

        <activity
            android:name="com.indong.choirfinder.activity.LoadingScreenActivity"
            android:hardwareAccelerated="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.indong.choirfinder.activity.StartActivity"
            android:hardwareAccelerated="true"/>
        <activity
            android:name="com.indong.choirfinder.activity.MainActivity"
            android:hardwareAccelerated="true"/>

    </application>


</manifest>