@file:JvmName("StringLib")//'StringLib' 라는 것으로 자바에서 접근 가능함.
package com.indong.choirfinder.library

class StringLib {

    fun isEmptyString(data:String): Boolean {
       return data.isEmpty()
    }
}